     pipeline {
        agent none
        stages {
         
          stage("build & sonarqube") {
            agent any
            steps {
              withSonarQubeEnv('LocalSonarqube') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
