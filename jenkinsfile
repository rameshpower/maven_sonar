     pipeline {
        agent none
        stages {
         
          stage("build & sonarqube") {
            agent any
            steps {
              withSonarQubeEnv('LocalSonarqube') {
                sh 'mvn sonar:sonar'
              }
            }
          }
        }
      }
