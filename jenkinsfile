     pipeline {
        agent none
        stages {
         
          stage("build & sonarqube") {
            agent any
            steps {
              withSonarQubeEnv('LocalSonaqube') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
